- API 模块采用 TypeScript 接口定义响应数据结构（如 `Category`, `ProductSpu`），确保前后端数据类型一致。
- 网络请求统一通过封装的 `request` 函数发起，自动处理 Bearer Token 认证及全局错误 Toast 提示。
- 页面组件优先使用 `<script setup lang="ts">` 语法糖，结合 `ref` 和 `onMounted` 管理响应式数据与生命周期。