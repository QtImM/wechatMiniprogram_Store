- 所有 API 响应必须统一包装为 `CommonResult<T>` 对象，包含 code、msg 和 data 字段。
- 数据库实体类需继承 `BaseDO` 以自动管理 `createTime`、`updateTime` 和逻辑删除字段 `deleted`。
- 持久层 Mapper 接口应继承 `BaseMapperX<T>` 以获得标准化的分页查询能力。
- 业务异常通过抛出 `ServerException` 触发全局处理器，系统异常由 `GlobalExceptionHandler` 统一捕获并返回标准错误码。