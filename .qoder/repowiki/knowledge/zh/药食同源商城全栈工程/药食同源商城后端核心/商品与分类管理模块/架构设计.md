- 采用经典三层架构：`controller`层区分`admin`（管理端）和`app`（小程序端）入口，`service`层封装业务逻辑，`dal`层通过MyBatis-Plus进行数据访问。
- 依赖方向明确：Controller -> Service -> Mapper (BaseMapperX) -> DataObject。
- 接口设计遵循REST规范，统一使用`CommonResult`包装返回结果，分页查询使用`PageParam`和`PageResult`。
- 数据对象`ProductSpuDO`继承自框架层的`BaseDO`，利用Lombok简化代码。