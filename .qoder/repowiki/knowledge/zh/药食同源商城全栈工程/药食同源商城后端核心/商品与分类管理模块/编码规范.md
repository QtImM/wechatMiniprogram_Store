- 所有Controller类均使用`@RequiredArgsConstructor`注入Service依赖，并返回统一的`CommonResult<T>`响应结构。
- Service层通过`LambdaQueryWrapper`构建动态查询条件，并利用MyBatis-Plus的`selectPage`方法实现分页逻辑。
- 数据访问层Mapper接口继承自框架提供的`BaseMapperX<T>`，无需编写基础CRUD的XML或注解SQL。