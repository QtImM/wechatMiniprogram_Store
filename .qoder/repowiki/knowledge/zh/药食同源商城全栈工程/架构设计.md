- **规格驱动编排**：以 `docs/superpowers/status.md` 为项目状态的唯一真相来源，协调 `shop_backend` 与 `shop_miniapp` 的迭代进度。
- **AI 协作契约**：根目录下的 `AGENTS.md`、`CLAUDE.md` 等文件定义了统一的 AI 交互工作流，确保跨模块开发时的上下文同步。
- **前后端解耦**：后端提供 RESTful API，前端通过 `shop-miniapp` 消费接口，两者通过明确的接口定义（Specs）进行集成。