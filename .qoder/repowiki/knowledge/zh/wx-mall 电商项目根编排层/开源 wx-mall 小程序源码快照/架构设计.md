本模块为只读快照，不包含可编译源码（`src/`、`pages/`、`utils/` 等目录均未检出），仅保留以下文件：
- `manifest.json`：uni-app 应用配置，声明目标平台为多端（`mp-weixin`、`mp-alipay`、`mp-baidu`、`mp-toutiao`、`mp-qq`、`h5`、`app-plus`），其中 `vueVersion: "2"` 表明该快照基于 Vue 2 + Vuex 的原生小程序实现。
- `README.md`：项目说明文档，记录了 HBuilderX 开发流程、Mock 模式开关（`useMock`）以及切换到真实后端的步骤。
- `unpackage/dist/`：HBuilderX 或微信开发者工具生成的构建产物目录（当前为空）。

依赖方向：本模块不依赖仓库内其他子模块，也不被其他模块引用，属于纯归档型 leaf 模块。